# 图片识别流程

## 适用场景

用户提供了一张 UI 截图或设计稿图片，要求根据图片内容生成对应页面。

---

## 强制执行步骤

### 步骤 1：分析图片（三级回退机制）

按以下优先级依次尝试，**任何一级成功即停止，不再继续下一级**：

---

#### 级别 1：图片识别 MCP 工具（优先）

如果环境中存在图片识别相关的 MCP 工具（如 `mcp_zai-mcp-server_ui_to_artifact`、`mcp_zai-mcp-server_analyze_image` 等），优先使用。

**分析提示词必须包含以下内容：**

```
请详细描述这张图片中的所有 UI 元素，包括：
1. 页面整体布局（是否有左侧导航、顶部搜索、中间表格等）
2. 搜索/筛选区域：所有输入框、下拉框、按钮
3. 表格区域：所有列名、数据示例
4. 操作按钮：新增、编辑、删除、导入、导出等
5. 特殊组件：状态开关、树形结构、标签页、分页等
6. 表单字段（如果有弹窗/抽屉中的表单）
```

如果 MCP 工具调用成功并返回有效结果 → **进入步骤 2**。

如果 MCP 工具不可用（未安装、连接失败）或返回结果无效 → **进入级别 2**。

---

#### 级别 2：模型自身视觉能力

如果当前模型具备多模态图片理解能力（可直接处理图片输入），则使用模型自身的视觉能力分析图片。

**使用方式：** 将图片直接作为输入，配合与级别 1 相同的分析提示词进行识别。

如果模型支持图片输入且成功识别 → **进入步骤 2**。

如果模型不具备图片理解能力或识别失败 → **进入级别 3**。

---

#### 级别 3：问询用户（最终回退）

如果以上两级均不可用，**无法识别图片内容**，必须使用 `AskUserQuestion` 工具告知用户并请求协助：

> **"当前环境无法识别图片。请提供以下信息以便生成页面："**
> - 页面整体布局描述（左侧导航、搜索区、表格区等）
> - 搜索/筛选字段列表
> - 表格列名及数据示例
> - 操作按钮列表
> - 是否有弹窗/抽屉表单

用户回答后，将用户的文字描述作为分析结果 → **进入步骤 2**。

### 步骤 2：整理分析结果

将 MCP 返回的分析结果整理为结构化的需求描述，包括：

| 类别 | 需整理的内容 |
|---|---|
| 页面布局 | 左侧树/右侧内容、标签页、搜索区、表格区等 |
| 搜索字段 | 字段名、类型（Input/Select/DatePicker 等）、占位文本 |
| 表格列 | 列名、字段名、宽度、特殊渲染（Tag/Switch/Link 等） |
| 操作按钮 | 工具栏按钮、行操作按钮、批量操作按钮 |
| 表单字段 | 字段名、类型、校验规则、默认值 |
| 特殊功能 | 批量操作、导入导出、状态切换、解锁等 |

### 步骤 3：判断是否为 CRUD 页面

根据分析结果判断：

| 判断条件 | 结论 | 后续动作 |
|---|---|---|
| 页面包含**表格 + 搜索/筛选 + 增删改查操作** | ✅ CRUD 页面 | 进入 **CRUD 生成流程**（`crud-generation.md`），将步骤 2 的结果作为需求输入 |
| 页面是仪表盘、详情页、图表页等非 CRUD 结构 | ❌ 非 CRUD 页面 | 继续本流程的步骤 4 |
| 页面结构复杂，无法确定 | ⚠️ 需确认 | 使用 `AskUserQuestion` 工具询问用户 |

### 步骤 4：非 CRUD 页面的生成流程

如果页面不是 CRUD 结构，根据图片内容生成对应页面。

#### 4.1 确定页面类型

| 页面类型 | 特征 | 参考模板 |
|---|---|---|
| **仪表盘/工作台** | 卡片、图表、统计数据、快捷入口 | `assets/page-template.md` |
| **详情页** | 展示单条数据的详细信息 | `assets/form-template.md`（只读模式） |
| **表单页** | 独立表单（非弹窗），如设置页、配置页 | `assets/form-template.md` |
| **图表页** | 以图表展示为主 | `assets/page-template.md` |
| **混合页面** | 包含多种组件，无法简单归类 | `assets/page-template.md` |

#### 4.2 确认需求

1. **确认接口来源**：使用 `AskUserQuestion` 询问用户接口从哪里获取（Apifox MCP / backend-mock 模拟数据 / 自定义 / 用户直接提供），具体流程参照 `crud-generation.md` 的步骤 2
2. **确认模块名称**：使用 `AskUserQuestion` 确认模块英文名称
3. **确认特殊功能**：使用 `AskUserQuestion` 确认特殊功能需求

#### 4.3 阅读参考文档

根据页面类型，阅读对应的参考文档（与 CRUD 流程相同的文档列表），参照 `crud-generation.md` 步骤 3。

**组件使用规则：** 参照 `SKILL.md` 中的"组件使用规则"。优先使用 Vben 封装组件；框架没有的组件直接使用 `tdesign-vue-next` 原生组件。不确定用法时，使用 `WebFetch` 抓取 TDesign 文档。

#### 4.4 生成文件

根据页面类型生成对应的文件结构。非 CRUD 页面的文件结构可能不同于标准 CRUD：

- **仪表盘/图表页**：可能只需要 `index.vue`，不需要 `form.vue` 和 `data.ts`
- **详情页**：可能需要 `index.vue` + `modules/detail.vue`
- **表单页**：可能只需要 `index.vue`

使用 `AskUserQuestion` 确认最终的文件结构后再生成。

#### 4.5 代码质量检查

参照 `crud-generation.md` 步骤 5 执行代码质量检查。

### 步骤 5：输出结果

生成完成后，按照标准格式输出总结。
